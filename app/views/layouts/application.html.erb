<!DOCTYPE html>
<html>
  <head>
    <title>SharpSpring</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <% if protect_against_forgery? %>
       <meta name="authenticity-token" id="authenticity-token" content="<%= form_authenticity_token %>" />
    <% end %>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poppins">
    <style>
    body,h1,h2,h3,h4,h5 {font-family: "Poppins", sans-serif}
    body {font-size:16px;}
    .w3-half img{margin-bottom:-6px;margin-top:16px;opacity:0.8;cursor:pointer}
    .w3-half img:hover{opacity:1}
    </style>
  </head>

  <body>
    <center>
    <% flash.each do |type, msg| %>
      <div style="color: black;">
        <%= msg %>
      </div>
    <% end %>
    </center>
    <!-- Sidebar/menu -->
    <nav class="w3-sidebar w3-red w3-collapse w3-top w3-large w3-padding" style="z-index:3;width:300px;font-weight:bold;" id="mySidebar"><br>
      <a href="javascript:void(0)" onclick="w3_close()" class="w3-button w3-hide-large w3-display-topleft" style="width:100%;font-size:22px">Close Menu</a>
      <div class="w3-container">
        <h3 class="w3-padding-64"><b>Noterizer</b></h3>
      </div>
      <div class="w3-bar-block">
        <%= link_to "Home", root_path, class: "w3-bar-item w3-button w3-hover-white" %>
        <% if current_user %>
          <%= link_to "Create Note", new_note_path, class: "w3-bar-item w3-button w3-hover-white" %>
          <%= link_to current_user.name, edit_user_registration_path, class: "w3-bar-item w3-button w3-hover-white" %>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "w3-bar-item w3-button w3-hover-white" %>
        <% end %>
      </div>
    </nav>
    <!-- Top menu on small screens -->
    <header class="w3-container w3-top w3-hide-large w3-red w3-xlarge w3-padding">
      <a href="javascript:void(0)" class="w3-button w3-red w3-margin-right" onclick="w3_open()">☰</a>
      <span>Noterizer</span>
    </header>
    <%= yield %>
  </body>
</html>
